#pragma once

#include <unordered_map>
#include <iostream>
#include <vector>
#include <string>
#include <bakanet.h>

#define log(str) std::cout << str << "\n";
#define input(ref) std::cin >> ref;

#define PORT 8080